# Provider logos for Reasoning Chain visualization

Drop SVG (or PNG) files into this folder using the exact filenames below.
The Generative AI page uses `<img>` tags with monogram fallbacks until files are present.

## Model layer

| Filename | Used on |
|----------|---------|
| `openai.svg` | Model row, research agent tool strip |
| `claude.svg` | Model row, synthesis tool strip |
| `gemini.svg` | Model row, finance agent replan |

## Knowledge layer

| Filename | Used on |
|----------|---------|
| `pinecone.svg` | Vector retrieval node |
| `qdrant.svg` | Hybrid search node |
| `neo4j.svg` | Graph relations node |

## Tool / delivery layer

| Filename | Used on |
|----------|---------|
| `slack.svg` | Output delivery row |
| `gmail.svg` | Output delivery row |
| `notion.svg` | Output delivery row (PDF export) |

Recommended size: 24×24px or square SVG viewBox. Prefer official brand marks where licensing permits.
